"""
Django settings for Rosetta project.

Generated by 'django-admin startproject' using Django 6.0.


核心配置文件
包含：
- 基础路径与密钥配置
- 应用注册 (INSTALLED_APPS)
- 中间件配置 (MIDDLEWARE)
- 模板与数据库配置
- 认证与密码策略
- 国际化与静态资源
- 第三方库集成 (Tailwind, DRF, Constance, etc.)
- 日志系统 (Loguru)
"""

import os
from pathlib import Path
from datetime import timedelta

# 构建项目内的路径: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent


# ==============================================================================
# 1. 核心安全设置 (Core Security Settings)
# ==============================================================================

# SECURITY WARNING: keep the secret key used in production secret!
# 在生产环境中，请确保 SECRET_KEY 保密且唯一。建议使用环境变量获取。
SECRET_KEY = os.environ.get(
    "DJANGO_SECRET_KEY",
    "django-insecure-598u(^sv4vtl1)@)uxmsz%&oeoxtgau09)5en^0#n!uz63$o@(",
)

# SECURITY WARNING: don't run with debug turned on in production!
# 生产环境请务必设置为 False，否则会暴露敏感信息。
DEBUG = True

ALLOWED_HOSTS = []

# Security Hardening
SECURE_BROWSER_XSS_FILTER = True
SECURE_CONTENT_TYPE_NOSNIFF = True
SESSION_COOKIE_SECURE = not DEBUG
CSRF_COOKIE_SECURE = not DEBUG
SESSION_COOKIE_HTTPONLY = True
# CSRF_COOKIE_HTTPONLY = True # Keep False to allow JS access for AJAX
DEFAULT_CHARSET = 'utf-8'
X_FRAME_OPTIONS = 'DENY' # Protect against clickjacking
REFERRER_POLICY = 'same-origin'

if not DEBUG:
    # HSTS Settings (Enable in production)
    SECURE_HSTS_SECONDS = 31536000  # 1 year
    SECURE_HSTS_INCLUDE_SUBDOMAINS = True
    SECURE_HSTS_PRELOAD = True
    SECURE_SSL_REDIRECT = True # Force HTTPS



# ==============================================================================
# 2. 应用配置 (Application Definition)
# ==============================================================================

INSTALLED_APPS = [
    # --- 本地应用 (Local Apps) ---
    "administration",         # 自定义管理后台
    
    # --- Django 内置应用 ---
    "django.contrib.admin",
    "django.contrib.auth",
    "django.contrib.contenttypes",
    "django.contrib.sessions",
    "django.contrib.messages",
    "django.contrib.staticfiles",
    "django.contrib.sites",   # 站点框架 (Required for comments, sitemaps)
    "django.contrib.sitemaps",# 站点地图
    
    # --- 第三方扩展 (Third Party Apps) ---
    "guardian",               # 对象级权限管理 (Object permissions)
    "tailwind",               # Tailwind CSS 集成
    "theme",                  # 前端主题应用 (DaisyUI)
    "django_browser_reload",  # 开发环境自动刷新
    "django_htmx",            # HTMX 支持
    "django_filters",         # 数据过滤器
    "import_export",          # 数据导入导出
    "captcha",                # 图形验证码
    "simple_history",         # 模型变更历史记录
    "rest_framework",         # REST API 框架
    "rest_framework_simplejwt", # JWT 认证
    "imagekit",               # 图片处理 (缩略图等)
    "watson",                 # 全文搜索引擎
    "compressor",             # 静态资源压缩与合并
    "meta",                   # SEO Meta 标签生成
    "constance",              # 动态配置系统
    "constance.backends.database",
    "chartjs",                # Chart.js 图表集成
    
    # --- 业务应用 (Business Apps) ---
    "blog.apps.BlogConfig",   # 博客核心业务
    "users.apps.UsersConfig", # 用户与认证系统
    "core.apps.CoreConfig",   # 核心通用功能
]

MIDDLEWARE = [
    "django.middleware.security.SecurityMiddleware",
    "whitenoise.middleware.WhiteNoiseMiddleware",  # Static file serving
    "django.contrib.sessions.middleware.SessionMiddleware",
    "django.middleware.common.CommonMiddleware",
    "django.middleware.csrf.CsrfViewMiddleware",
    "django.contrib.auth.middleware.AuthenticationMiddleware",
    "django.contrib.messages.middleware.MessageMiddleware",
    "django.middleware.clickjacking.XFrameOptionsMiddleware",
    
    # --- 自定义与第三方中间件 ---
    "django_htmx.middleware.HtmxMiddleware",             # HTMX 请求处理
    "simple_history.middleware.HistoryRequestMiddleware", # 自动记录请求用户到历史记录
    "watson.middleware.SearchContextMiddleware",         # Watson 搜索上下文自动更新
]

# 开发环境添加浏览器自动刷新中间件
if DEBUG:
    MIDDLEWARE += [
        "django_browser_reload.middleware.BrowserReloadMiddleware",
    ]

ROOT_URLCONF = "Rosetta.urls"

TEMPLATES = [
    {
        "BACKEND": "django.template.backends.django.DjangoTemplates",
        "DIRS": [BASE_DIR / "templates"], # 全局模板目录
        "APP_DIRS": True,
        "OPTIONS": {
            "context_processors": [
                "django.template.context_processors.debug",
                "django.template.context_processors.request",
                "django.contrib.auth.context_processors.auth",
                "django.contrib.messages.context_processors.messages",
                # 自定义上下文处理器
                "core.context_processors.site_settings", # 全局站点设置
                "constance.context_processors.config",   # 动态配置注入模板
            ],
        },
    },
]

WSGI_APPLICATION = "Rosetta.wsgi.application"


# ==============================================================================
# 3. 数据库 (Database)
# ==============================================================================
# https://docs.djangoproject.com/en/6.0/ref/settings/#databases

DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.sqlite3",
        "NAME": BASE_DIR / "db.sqlite3",
    }
}


# ==============================================================================
# 4. 认证与用户 (Authentication & Users)
# ==============================================================================

# 指定自定义用户模型
AUTH_USER_MODEL = "users.User"

# 认证后端配置
AUTHENTICATION_BACKENDS = (
    "django.contrib.auth.backends.ModelBackend",   # Django 默认后端
    "guardian.backends.ObjectPermissionBackend",   # Guardian 对象级权限后端
)

# 密码验证策略
AUTH_PASSWORD_VALIDATORS = [
    {
        "NAME": "django.contrib.auth.password_validation.UserAttributeSimilarityValidator",
    },
    {
        "NAME": "django.contrib.auth.password_validation.MinimumLengthValidator",
    },
    {
        "NAME": "django.contrib.auth.password_validation.CommonPasswordValidator",
    },
    {
        "NAME": "django.contrib.auth.password_validation.NumericPasswordValidator",
    },
]

# 登录重定向配置
LOGIN_REDIRECT_URL = "/"      # 登录后默认跳转首页
LOGOUT_REDIRECT_URL = "/"     # 登出后跳转首页
LOGIN_URL = "users:login"     # 登录页面 URL 名称


# ==============================================================================
# 5. 国际化与本地化 (I18N & L10N)
# ==============================================================================

LANGUAGE_CODE = "zh-hans" # 默认语言：简体中文
TIME_ZONE = "Asia/Shanghai" # 默认时区：上海
USE_I18N = True # 启用国际化
USE_TZ = True # 启用时区支持


# ==============================================================================
# 6. 静态文件与媒体文件 (Static & Media)
# ==============================================================================

STATIC_URL = "static/"
STATIC_ROOT = BASE_DIR / "staticfiles"  # collectstatic 收集目录

# 静态文件查找器配置
STATICFILES_FINDERS = (
    "django.contrib.staticfiles.finders.FileSystemFinder",
    "django.contrib.staticfiles.finders.AppDirectoriesFinder",
    "compressor.finders.CompressorFinder", # 支持 Django Compressor
)

# Static file storage with cache busting and compression
STATICFILES_STORAGE = "whitenoise.storage.CompressedManifestStaticFilesStorage"

MEDIA_URL = "/media/"
MEDIA_ROOT = BASE_DIR / "media" # 用户上传文件存储目录


# ==============================================================================
# 7. 第三方库详细配置 (Third-Party Configurations)
# ==============================================================================

# 7.1 Tailwind CSS
TAILWIND_APP_NAME = "theme"
NPM_BIN_PATH = "C:\\Program Files\\nodejs\\npm.cmd" # 请根据实际开发环境修改路径

# 7.2 Django Sites Framework
SITE_ID = 1

# 7.3 DRF & JWT Settings
REST_FRAMEWORK = {
    "DEFAULT_AUTHENTICATION_CLASSES": (
        "rest_framework_simplejwt.authentication.JWTAuthentication", # 优先使用 JWT
        "rest_framework.authentication.SessionAuthentication",       # 允许 Session 认证（用于调试页面）
    )
}

SIMPLE_JWT = {
    "ACCESS_TOKEN_LIFETIME": timedelta(minutes=60),
    "REFRESH_TOKEN_LIFETIME": timedelta(days=1),
    "ROTATE_REFRESH_TOKENS": False,
    "BLACKLIST_AFTER_ROTATION": False,
    "UPDATE_LAST_LOGIN": True,
}

# 7.4 Django Constance (动态配置)
CONSTANCE_BACKEND = "constance.backends.database.DatabaseBackend"
CONSTANCE_CONFIG = {
    # General
    "SITE_NAME": ("Rosetta Blog", "站点名称"),
    "SITE_DESCRIPTION": ("A modern Django blog.", "站点描述"),
    "SITE_KEYWORDS": ("blog, django, python", "SEO 关键词"),
    "SHOW_SITE_LOGO": (True, "是否显示站点 Logo"),
    "SITE_LOGO": ("/static/core/img/logo.png", "站点 Logo URL"),
    "SITE_FAVICON": ("/static/core/img/favicon.ico", "站点 Favicon URL"),
    
    # Admin UI
    "SITE_HEADER": ("Rosetta Dashboard", "后台头部标题"),
    "SITE_ADMIN_SUFFIX": (" - Rosetta Dashboard", "后台页面标题后缀"),
    "ADMIN_NAVBAR_TITLE": ("Rosetta 管理后台", "后台导航栏标题"),
    "DASHBOARD_WELCOME_TEXT": ("这里是您的站点概览，祝您有美好的一天。", "仪表盘欢迎语"),
    "DASHBOARD_WELCOME_WORDS": ("['Creator', 'Admin', 'Master', 'Manager']", "仪表盘欢迎词 (Flip Words)"),
    
    # Footer
    "FOOTER_TEXT": ("© 2026 Rosetta Blog", "页脚文本"),
    "BEIAN_CODE": ("", "ICP 备案号"),
    
    # Social & Contact
    "GITHUB_URL": ("", "GitHub 链接"),
    "X_URL": ("", "X (Twitter) 链接"),
    "BILIBILI_URL": ("", "Bilibili 链接"),
    "CONTACT_EMAIL": ("", "联系邮箱"),
    
    # Email Settings
    "SMTP_HOST": ("smtp.qq.com", "SMTP 服务器地址"),
    "SMTP_PORT": (465, "SMTP 端口"),
    "SMTP_USER": ("", "SMTP 用户名"),
    "SMTP_PASSWORD": ("", "SMTP 密码/授权码"),
    "SMTP_USE_TLS": (True, "使用 SSL/TLS"),
    "SMTP_FROM_EMAIL": ("", "默认发件人邮箱"),
    
    # Feature Switches
    "MAINTENANCE_MODE": (False, "开启维护模式"),
    "ENABLE_COMMENTS": (True, "开启评论功能"),
    "ENABLE_REGISTRATION": (True, "开启用户注册"),
    "ENABLE_EMAIL_NOTIFICATIONS": (False, "开启邮件通知"),
    
    # Custom Code
    "EXTRA_HEAD_CODE": ("", "自定义 Head 代码 (CSS/JS)"),
    "EXTRA_FOOTER_CODE": ("", "自定义 Footer 代码 (JS)"),
}
CONSTANCE_CONFIG_FIELDSETS = {
    "基本设置": (
        "SITE_NAME",
        "SITE_DESCRIPTION",
        "SITE_KEYWORDS",
        "SHOW_SITE_LOGO",
        "SITE_LOGO",
        "SITE_FAVICON",
        "BEIAN_CODE",
        "FOOTER_TEXT",
    ),
    "后台界面": (
        "SITE_HEADER",
        "SITE_ADMIN_SUFFIX",
        "ADMIN_NAVBAR_TITLE",
        "DASHBOARD_WELCOME_TEXT",
        "DASHBOARD_WELCOME_WORDS",
    ),
    "社交与联系": (
        "GITHUB_URL", 
        "X_URL",
        "BILIBILI_URL",
        "CONTACT_EMAIL",
    ),
    "邮件服务": (
        "SMTP_HOST",
        "SMTP_PORT",
        "SMTP_USER",
        "SMTP_PASSWORD",
        "SMTP_USE_TLS",
        "SMTP_FROM_EMAIL",
        "ENABLE_EMAIL_NOTIFICATIONS",
    ),
    "功能开关": (
        "MAINTENANCE_MODE", 
        "ENABLE_COMMENTS",
        "ENABLE_REGISTRATION",
    ),
    "自定义代码": (
        "EXTRA_HEAD_CODE",
        "EXTRA_FOOTER_CODE",
    ),
}

# 7.5 Django Compressor
COMPRESS_ENABLED = not DEBUG  # 开发环境建议关闭压缩，避免样式加载缓存问题
COMPRESS_OFFLINE = False

# 7.6 Django Meta (SEO)
META_SITE_PROTOCOL = "http"
META_SITE_DOMAIN = "localhost:8000"
META_USE_OG_PROPERTIES = True
META_USE_TWITTER_PROPERTIES = True

# 7.7 Loguru Logging (日志系统)
LOGGING = {
    "version": 1,
    "disable_existing_loggers": False,
    "handlers": {
        "intercept": {
            "level": "INFO",
            "class": "core.logging.InterceptHandler", # 自定义拦截器
        },
    },
    "loggers": {
        "django": {
            "handlers": ["intercept"],
            "level": "INFO",
            "propagate": True,
        },
        "django.db.backends": {
            "handlers": ["intercept"],
            "level": "WARNING",  # 减少 SQL 日志噪音
            "propagate": False,
        },
    },
}

# 配置 Loguru 文件日志
LOG_DIR = BASE_DIR / "logs"
if not LOG_DIR.exists():
    try:
        LOG_DIR.mkdir(parents=True, exist_ok=True)
    except Exception:
        pass

if not DEBUG:
    from loguru import logger
    logger.add(
        LOG_DIR / "rosetta.log",
        rotation="10 MB",
        retention="30 days",
        level="WARNING",
        compression="zip",
        enqueue=True,
    )
else:
    from loguru import logger
        # 开发环境使用 debug.log，记录更详细的信息
    logger.add(
        LOG_DIR / "debug.log",
        level="DEBUG",
    )

# 7.8 Email Backend (Custom)
EMAIL_BACKEND = "core.backends.ConstanceEmailBackend"

